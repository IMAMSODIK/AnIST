## Implementasi Business Rule: Cybersecurity Incident (Network Detection Log)

### Masalah yang Akan Diperbaiki

1. **Bug formula `target=0` di KPIService** — saat ini terbalik: `$realisasi > 0 ? 100 : 0`. Harusnya: target=0, realisasi=0 → 100%, target=0, realisasi>0 → 0% (zero-tolerance binary).
2. **Prompt AI generik** — `getCybersecurityPrompt()` tidak menyuruh Gemini membaca tabel dan menghitung baris. Untuk studi kasus ini, AI harus memahami kolom "action" dan menghitung baris yang action-nya ≠ "blocked".
3. **Prompt utama mengasumsikan completion 0-1** — line 44: *"If evidence shows completion, realisasi = 1"*. Untuk KPI count-based (incident), realisasi harus berupa jumlah integer (0, 1, 2, dst).
4. **Formula `calculateLowerIsBetterAchievement` saat ini menganggap target=0 → realisasi<=0 = 100, else = 0** — tapi kode di `calculateAchievement()` intercept target=0 SEBELUM masuk ke lower-is-better, jadi logicnya double. Perlu disatukan.

---

### Perubahan File

#### 1. `app/Services/KPIService.php` — Fix formula target=0

**Di `calculateAchievement()`:**
- Hapus special case `if ($target == 0)` di line 68-70 (yang terbalik)
- Di `calculateLowerIsBetterAchievement()`, logic `target <= 0` sudah benar: `realisasi <= 0 ? 100 : 0` → ini mengimplementasikan zero-tolerance binary secara natural
- Tambahkan komentar bahwa untuk "lower is better" dengan target=0, ini zero-tolerance: 0 incident = 100%, any incident = 0%
- **Pastikan** measurement "cybersecurity incident" punya `formula` = "lower is better" (ini akan di-set oleh user saat create measurement, bukan kode)

#### 2. `app/AI/PromptManager.php` — Tambah sub-routing "incident count"

**Di `getBasePrompt()`:**
- Setelah cek `cybersecurity`, tambah cek lebih spesifik: `str_contains($name, 'incident')` → panggil `getCybersecurityIncidentPrompt()`
- Ini memastikan measurement "Cybersecurity Incident" mendapat prompt yang berbeda dari cybersecurity umum (audit/compliance)

**Method baru `getCybersecurityIncidentPrompt()`:**
```php
protected function getCybersecurityIncidentPrompt(): string
{
    return <<<'PROMPT'
# Evidence Analysis: Cybersecurity Incident Count

You are a cybersecurity incident analyst. Your task is to COUNT the number of successful security incidents from a network detection log.

## What Counts as an "Incident"
An incident is counted when a network event's **"action" column value is NOT "blocked"** (case-insensitive). This means the attack successfully penetrated the system.

## Instructions
1. Read the uploaded evidence (network detection log) carefully
2. Look at every row/entry in the log table
3. For EACH row, check the "action" column value
4. If action = "blocked" → NOT an incident (attack was stopped)
5. If action = anything OTHER than "blocked" (e.g., "allowed", "passed", "detected", "logged", "no action", etc.) → this IS an incident (attack succeeded)
6. Count ALL rows where action ≠ "blocked"
7. The total count is your realisasi value

## Critical Rules
- The realisasi value must be an INTEGER representing the COUNT of incidents (0, 1, 2, 3, etc.)
- Count EVERY row in the evidence, do not skip any entries
- If the evidence is empty or has no table, set evidence_valid = false and realisasi = 0
- The "action" column check is case-insensitive ("Blocked", "BLOCKED", "blocked" all mean blocked)
PROMPT;
}
```

#### 3. `app/AI/PromptManager.php` — Perbaiki prompt utama agar mendukung count-based realisasi

**Di `generatePrompt()`, bagian "Important Rules":**
- Tambahkan openu check: jika measurement mengandung "incident" atau formula = "lower is better", realisasi adalah **count/jumlah** (integer)
- Tanpa mengubah perilaku measurement lain (backward compatible)

#### 4. Tidak ada perubahan database atau model — Semua sudah ada
- `formula` column di tabel `measurements` sudah ada → user isi "lower is better" saat create measurement
- Target=0 → user isi target 0 di master target per quarter
- Realisasi → AI akan menghasilkan count integer
- Score & achievement → KPIService sudah menangani "lower is better" dengan target=0 → zero-tolerance binary

#### 5. Tidak ada perubahan view/controller
- Route `/reports` sudah menampilkan achievement% dan status dari KPIService
- AI Insight (Call #2) sudah otomatis ter-generate setelah evidence diproses

---

### Cara Penggunaan (User Flow)

1. **Admin creates measurement** baru:
   - Perspective: Customer
   - Objective: Operational Excellence
   - Measurement: Cybersecurity Incident
   - Formula: `lower is better`
   - Unit: `incidents`

2. **Admin sets targets**: Q1-Q4 = 0

3. **Admin uploads evidence**: `log-network-detection_aprile2026.pdf`

4. **AI reads the PDF** → sees table with "action" column → counts rows where action ≠ "blocked" → outputs realisasi = 2 (misalnya 2 serangan berhasil)

5. **Laravel calculates**: target=0, realisasi=2, formula="lower is better" → achievement = 0% (zero-tolerance)

6. **AI Insight** (Call #2) otomatis menjelaskan mengapa KPI belum tercapai dan memberi rekomendasi

7. **Reports page** menampilkan achievement 0%, expandable row dengan insight

---

### Ringkasan File yang Diubah

| File | Perubahan | Risiko |
|---|---|---|
| `app/Services/KPIService.php` | Fix bug formula target=0 (hapus special case terbalik, serahkan ke lower-is-better) | **Rendah** — logic lower-is-better sudah benar untuk target=0 |
| `app/AI/PromptManager.php` | Tambah sub-routing `incident` + method `getCybersecurityIncidentPrompt()` + perbaiki realisasi rule | **Rendah** — hanya menambah cabang baru, tidak mengubah existing |

**Tidak ada migrasi, tidak ada file baru, tidak ada perubahan UI.**